// This file includes all the dom element access in the website

export const generatePaletteBtn = window.document.body.querySelector("#generatePaletteBtn");

export const items = window.document.body.querySelectorAll(".items");
export const colorHolder = window.document.body.querySelectorAll(".colorHolder");
export const colorCodeHolder = window.document.body.querySelectorAll(".colorCodeHolder");

export const copyPopup = window.document.body.querySelector("#copyPopup");